# Docs system

How the two documentation systems work and how to edit them

 Internal working note — not part of the public Nexus language reference.

## Current TODO

1. Migrate remaining presentation projects to `project.mode: presentation_yaml` with a local `Makefile`.

2. Use `nexus build presentations docs/publishing` or `make presentations` from `docs/publishing/` as the canonical batch build path.

3. Replace the dummy runner in `calyrai_modelling/scripts/run_module.py` with a real module dispatcher.

4. Implement `calyrai_modelling/modules/cryoem_patch/scripts/` as the first real modelling module.

5. After more presentation projects are migrated, do the larger physical cleanup of `docs/publishing/`.

---

## Current operational surfaces

The repo now has two concrete internal build surfaces that should be treated as active.

### YAML-backed presentations

| Purpose | Canonical surface |
| --- | --- |
| Canonical builder | docs/publishing/tools/presentation_yaml/build_presentation_from_yaml.py |
| Starter template | docs/publishing/templates/presentation_yaml_project/ |
| Build one presentation | nexus build presentation <project-root> or make presentation |
| Build all YAML presentations | nexus build presentations docs/publishing or make presentations from docs/publishing/ |
| Current real example | docs/publishing/Calyr_ai_syntax_full_docs/ |

### `calyrai_modelling` scaffold

| Purpose | Canonical surface |
| --- | --- |
| Scaffold root | calyrai_modelling/ |
| Bootstrap script | bootstrap_calyrai_modelling.py |
| List modules | nexus modelling ls |
| Run a module | nexus modelling run cryoem_patch |
| Current limitation | calyrai_modelling/scripts/run_module.py still writes a registration record and needs a real dispatcher. |

## Two separate systems

The workspace maintains two distinct documentation systems that serve different audiences.

|  | MkDocs hub (internal) | This site (public) |
| --- | --- | --- |
| What it is | Local dev-facing docs | Public Nexus language reference |
| Source files | docs/**/*.md | apps/homepage/docs/*.html + data/docs.js |
| Run via | mkdocs serve → localhost:8000 | Open docs.html in a browser |
| Nav config | mkdocs.yml — nav: section | apps/homepage/data/docs.js — window.CALYR_DOCS |
| Format | Markdown | HTML <article> fragments |
| URL pattern | /apps/terminal/install/ | docs.html#api/python |
| Audience | Internal contributors | Nexus language users |

---

## System 1 — MkDocs hub

### File layout

```text
docs/                      ← docs_dir in mkdocs.yml
  index.md
  apps/
    terminal/              ← one folder per app
      index.md  install.md  operators.md  architecture.md
  nexus/                   ← API reference (mirrored from here)
    api_python.md  api_cli.md
  hub/
    nexus.md               ← guided reading path
    navigator.md  all_pages.md
  notes/                   ← internal notes (this file's Markdown source)
  publishing/
  _repo/                   ← auto-generated symlinks into workspace
mkdocs.yml                 ← nav + theme
```

### Adding a page

1. Create the `.md` file under `docs/`.

2. Add an entry to `mkdocs.yml`:

```text
nav:
  - My Section:
      - My Page: path/to/my_page.md
```

3. Run `mkdocs serve` — hot-reloads automatically.

### Serve / build

```text
cd /path/to/Calyr
mkdocs serve          # → http://localhost:8000
mkdocs build          # → build/docs_site/
```

### Known build issue

`mkdocs build --strict` fails on broken `docs/_repo/` symlinks auto-generated by `tools/repo/build_docs_hub.py` where the targets no longer exist (e.g. `_repo/apps/homepage/pages_src/`). Non-strict builds and `mkdocs serve` are unaffected. Regenerate with:

```text
python3 tools/repo/build_docs_hub.py
```

---

## System 2 — This site (public homepage docs)

### File layout

```text
apps/homepage/
  docs.html              ← shell (sidebar + content panels only)
  data/docs.js           ← window.CALYR_DOCS navigation tree
  docs/                  ← one HTML fragment per page
    api_python.html  api_cli.html  language_syntax.html …
  js/docs.js             ← sidebar builder, hash router, prev/next
  css/docs.css
```

### Topic planner layout (internal scheduling surface)

 A dedicated internal UI now exists to define topic work and meaning with circle controls, generate a daily/weekly plan, and sync that plan to Google Calendar through the administration sync script.

```text
apps/homepage/pages/topic_planner.html   ← page shell + planner sections
apps/homepage/css/topic_planner.css     ← planner-specific visual system
apps/homepage/js/topic_planner.js       ← circles, scoring, day-plan generation, JSON export

administration/google_calendar_sync.py  ← --sync-topic-plan + --topic-plan-json ingestion
administration/google_calendar_config.json
administration/google_calendar_config.example.json
```

#### UI sections on topic_planner.html

| Section | Purpose |
| --- | --- |
| Topic create row | Add new topics dynamically. |
| Topic cards | Per-topic circle controls for work and meaning. |
| Planner actions | Generate plan, export topic_plan.json, copy sync command. |
| Day plan panel | Shows generated assignments for MO–SU. |
| Calendar sync note | Operator command to push plan into Google Calendar. |

#### Scheduling logic contract

- Each topic has two values: `work` (effort) and `meaning` (importance).

- Planner score is weighted to keep semantic priority dominant.

- Output is a `topic_plan` JSON object with `weekday_labels`, `daily_block`, and `review_block`.

- Google Calendar sync accepts this JSON directly via `--topic-plan-json`.

#### Command path (UI export → Calendar sync)

```text
# 1) Export topic_plan.json from the planner UI
# 2) Validate without writing to Google
python administration/google_calendar_sync.py \
  --config administration/google_calendar_config.json \
  --sync-topic-plan \
  --topic-plan-json /path/to/topic_plan.json \
  --dry-run

# 3) Write recurring events to Google Calendar
python administration/google_calendar_sync.py \
  --config administration/google_calendar_config.json \
  --sync-topic-plan \
  --topic-plan-json /path/to/topic_plan.json
```

### `calyr.vox` layout (internal synthetic voice-over surface)

 A dedicated internal voice system now exists to turn slide snippets into narrated outputs: `slides -> speaker text -> TTS audio -> timing -> video export`. It is available both as a Python pipeline and as a one-file browser studio.

```text
apps/homepage/vox.html                       ← login-gated internal entrypoint (homepage surface)
apps/homepage/js/vox_gate.js                ← auth/session gate for Vox page
apps/homepage/pages/vox_studio.html         ← embedded studio content served from homepage path

apps/vox/vox_studio.html                     ← canonical standalone studio source
apps/vox/calyr_vox.py                        ← CLI orchestrator (speak, synth, video, all)
apps/vox/speech_generator.py                 ← LLM speech generation (natural narration)
apps/vox/tts_engine.py                       ← TTS dispatcher (OpenAI + ElevenLabs)
apps/vox/timing_engine.py                    ← duration estimation + ffprobe timing
apps/vox/video_exporter.py                   ← ffmpeg segment render + concat
apps/vox/README.md                           ← operational usage contract

calyrai_modelling/snippets/slides/*.yaml     ← slide snippet inputs
output/vox/                                  ← voiced yaml + audio + manifest + final mp4
```

#### Voice systems (both supported)

| System | Use case | Required keys | Notes |
| --- | --- | --- | --- |
| Generic voice (OpenAI TTS) | Fast default narration for immediate production runs. | OPENAI_API_KEY | Stable quality, no cloning required, selectable stock voices. |
| Cloned voice (ElevenLabs) | Personalized delivery where speaker identity should match your own voice. | ELEVENLABS_API_KEY + voice_id | Requires recording upload first; then use returned voice_id for synthesis. |

#### HTML studio tabs and responsibilities

| Tab | Purpose |
| --- | --- |
| Einrichtung | Provider selection, API key capture, connection test. |
| Stimme einspielen | Microphone recording or file upload, ElevenLabs clone creation, preview playback. |
| Slides bauen | Create/edit snippet-style slides (id, title, bullets, tone, duration). |
| Generieren | Generate speaker text via LLM, then synthesize per-slide audio via selected provider. |
| Export | Export voiced YAML/manifests/audio and provide ffmpeg command path for mp4 assembly. |

#### Access mode (internal/login)

- Public homepage links should point to `vox.html`, not directly to the raw studio file.

- `vox.html` shows an auth gate and reveals the studio only after login token/session is present.

- Login handoff follows the same token/session pattern as Citizen (`?token=...&user=...` or existing local session).

#### Slide snippet contract for `calyr.vox`

```text
id: slide_legacy_reporting_001
type: slide
title: Legacy Reporting System
bullets:
  - Oracle Warehouse Builder
  - SAP BusinessObjects
  - 20 years legacy
tone: professional
duration: 30s
```

#### Speech generation rules (internal contract)

- Do not read bullet points verbatim; synthesize into natural narration.

- Respect tone (for example professional, scientific, casual, motivational).

- Honor duration guidance; default timing model assumes ~150 words per minute.

- Write speaker text back to voiced YAML for reproducible regeneration.

#### Voice cloning operator path (ElevenLabs)

```text
# 1) Record or upload clean speech sample in the studio
# 2) Create clone via ElevenLabs endpoint
POST https://api.elevenlabs.io/v1/voices/add

# 3) Persist returned voice_id
ELEVENLABS_VOICE_ID=<your_voice_id>

# 4) Synthesize with cloned voice
python apps/vox/calyr_vox.py all calyrai_modelling/snippets/slides/ \
  --provider elevenlabs \
  --voice $ELEVENLABS_VOICE_ID \
  --out output/vox
```

#### CLI path (batch generation)

```text
# Full run: slides -> speech -> audio -> video
python apps/vox/calyr_vox.py all calyrai_modelling/snippets/slides/ --out output/vox

# Speech only (no TTS/video)
python apps/vox/calyr_vox.py all calyrai_modelling/snippets/slides/ --dry-run

# Stepwise mode
python apps/vox/calyr_vox.py speak calyrai_modelling/snippets/slides/slide_legacy_reporting_001.yaml
python apps/vox/calyr_vox.py synth output/vox/voiced/slide_legacy_reporting_001.yaml --provider openai
python apps/vox/calyr_vox.py video output/vox/manifest.json
```

#### Security and storage notes

- The browser studio stores API keys in local browser storage for internal convenience; do not use shared browser profiles.

- For production operators, prefer environment variables and CLI execution in a controlled shell.

- Do not commit API keys, raw samples, or generated tokens into repository history.

### Adding a page

1. Create `apps/homepage/docs/<section>_<page>.html`:

```text
<article class="doc-article">
  <h1>Page Title</h1>
  <p class="doc-subtitle">One-liner</p>
  <h2>Section</h2>
  <p>Content</p>
</article>
```

2. Register it in `apps/homepage/data/docs.js`:

```text
{ id: 'my_page', title: 'My Page', src: 'docs/section_my_page.html' }
```

3. Live immediately at `docs.html#section/my_page` — no build step.

### Naming convention

`docs/<section_id>_<page_id>.html` — matches the `id` fields in `docs.js`.

### Content addressing standard (structure-first)

 For publication surfaces and long-lived content, prefer structure-driven paths over concatenated file names like `name_name2_.html`. Paths should express meaning and hierarchy.

```text
content/
  papers/
    saxs-unified/
      index.html
  slides/
    okto-intro/
      index.html
  concepts/
    pdd-duality/
      index.html
```

 Rule: each page lives in its own directory with an `index.html` entrypoint. Use lowercase, hyphen-separated slugs (for example `saxs-unified`, `okto-intro`).

### Object identity for Okto

Store an explicit object identity alongside each content page:

```text
id: paper_saxs_unified
slug: saxs-unified
type: paper
```

 Routing can then be generated deterministically via: `content/{type}/{slug}/index.html`.

### Migration strategy

1. Map legacy names (`name_name2_.html`) to semantic slugs (`saxs-unified`).

2. Move into structured folders (for example `content/papers/saxs-unified/index.html`).

3. Add optional redirect pages for legacy links where external references exist.

4. Mirror the same structure under `archive/` for clean historical retention.

---

## Keeping both systems in sync

Key pages exist in both systems. When you update one, update the other.

| Topic | MkDocs path | Homepage path |
| --- | --- | --- |
| Python API | docs/nexus/api_python.md | docs/api_python.html |
| CLI reference | docs/nexus/api_cli.md | docs/api_cli.html |
| Terminal operators | docs/apps/terminal/operators.md | docs/inst_md.html (LAMMPS layer) |

MkDocs versions carry a "mirrors" notice and a backlink to the homepage version.

---

## Quick reference — which file to edit

| I want to… | Edit this |
| --- | --- |
| Fix a typo in the terminal install guide | docs/apps/terminal/install.md |
| Add a new operator to the DSL reference | docs/apps/terminal/operators.md + apps/terminal/backend/executor.py |
| Add a page to the public Nexus docs (here) | apps/homepage/docs/<file>.html + apps/homepage/data/docs.js |
| Add a new section to the MkDocs hub | docs/<section>/ + mkdocs.yml |
| Change sidebar order here | apps/homepage/data/docs.js — reorder entries |
| Build one YAML-backed presentation | nexus build presentation <project-root> or the project Makefile |
| Build all YAML-backed presentations | docs/publishing/Makefile or nexus build presentations docs/publishing |
| Run a modelling module | nexus modelling run <module-id> or calyrai_modelling/scripts/run_module.py |
| Rebuild _repo/ symlinks | python3 tools/repo/build_docs_hub.py |
